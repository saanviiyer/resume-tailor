# Copy this file to .env and fill in your key to enable real Claude calls.
# If ANTHROPIC_API_KEY is unset, the server runs in MOCK MODE (fully usable, no key needed).
ANTHROPIC_API_KEY=

# Optional: override the server port (default 3001)
PORT=3001
